export interface Conversation {
    id: string;
    offer_id: string;
    poster_id: string;
    interested_user_id: string;
    is_active: boolean;
    last_message_at: string;
    created_at: string;
}